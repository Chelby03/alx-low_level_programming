#include <stdio.h>

/**
 * main- The function in which all c code goes under
 * puts: writes a string to the standard output
 * Return: 0
 */

int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
